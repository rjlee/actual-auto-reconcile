name: Release

on:
  push:
    branches: [ main ]

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: rjlee/actual-auto-ci/.github/workflows/release.yml@v1
    with:
      node-version: '20'
      package-manager: npm
